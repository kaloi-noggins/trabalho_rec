const net = require("net");

const port = 9000;
const host = "127.0.0.1";
const participantes = []

const server = net.createServer((socket) => {
    // Analisar tipo de mensagem
    // Criar uuid para cara participante
});